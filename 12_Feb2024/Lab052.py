# functions

# built in functions
# Some built in functions are given below
result = max(2,5)
num = int(input("Enter the number"))
import math

fact = math.factorial(5)
print("factorial",fact)

# functions ==> Block of code which will help you to execute some task

# define a f(n)
# call a f(n)

# built in f(n) not need to define, only need to call
# return and non return fuction  